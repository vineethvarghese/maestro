#!/bin/sh

$(dirname "$0")/../sbt ';set credentials in ThisBuild += Credentials(file("/etc/sbt.credentials")) ; set publishTo := Some("commbank-releases" at "http://commbank.artifactoryonline.com/commbank/ext-releases-local"); set publishMavenStyle := true; +publish'
